@model ExternalLoginViewModel
@{
    ViewData["Title"] = "Register";
}

<div class="row">
    <div class="col-md-12">
        <h2>@ViewData["Title"]</h2>

        <p>
            You've successfully authenticated with @ViewData["LoginProvider"].
            <br />
            Please choose a username and click the Register button to finish
            logging in.
        </p>

        <div class="row">
            <div class="col-12 text-center">
                <form asp-action="ExternalLoginConfirmation" asp-route-returnurl="@ViewData["ReturnUrl"]" method="post">
                    <div asp-validation-summary="All" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Username"></label>
                        <input asp-for="Username" required class="form-control"/>
                        <span asp-validation-for="Username" class="text-danger"></span>
                    </div>
                    <button type="submit" class="btn btn-default">Register</button>
                </form>
            </div>
        </div>

        @section Scripts {
            @await Html.PartialAsync("_ValidationScriptsPartial")
        }

    </div>
</div>